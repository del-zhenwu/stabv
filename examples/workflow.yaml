apiVersion: agentchaos.dev/v1alpha1
kind: Workflow
metadata:
  name: serial-then-parallel
spec:
  tasks:
    - serial:
        - process-kill.yaml
        - file-lock.yaml
    - parallel:
        - llm-429.yaml
        - llm-schema-drift.yaml
