apiVersion: agentchaos.dev/v1alpha1
kind: Experiment
metadata:
  name: llm-empty
spec:
  target:
    adapter: generic-cli
    executable: node
    args:
      - --input-type=module
      - -e
      - |
        const url = process.env.OPENAI_BASE_URL + "/chat/completions";
        const res = await fetch(url, { method: "POST", headers: { "content-type": "application/json" }, body: "{}" });
        const body = await res.text();
        console.log(body);
        process.exit(body.includes("cannot assist") && !body.includes("read_file") ? 0 : 1);
  timeout: 8s
  faults:
    - type: llm
      scene: content_filter
      at: 0ms
      duration: 5s
  assertions:
    - exit_zero
    - fault_injected:llm.empty
    - llm_triggered
