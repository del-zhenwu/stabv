apiVersion: agentchaos.dev/v1alpha1
kind: Workload
metadata:
  name: probe-idle
spec:
  target:
    adapter: generic-cli
    executable: node
    args:
      - -e
      - "setTimeout(() => console.log('idle done'), 500);"
  timeout: 8s
